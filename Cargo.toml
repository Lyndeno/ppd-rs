[package]
name = "ppd"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
zbus = {git = "https://github.com/lyndeno/zbus", branch = "rename_value_clean" }
futures = "0.3.*"
clap = { version = "4.*.*", features = ["derive"] }
